#include "Snake.hpp"

#include <cassert>

void Snake::GetInput()
{
	assert(!"Snake::GetInput");
}
void Snake::Update()
{
	assert(!"Snake::Update");
}
void Snake::Draw(SDL_Surface* target) const
{
	assert(!"Snake::Draw");
}
bool Snake::IsDead() const
{
	assert(!"Snake::Dead");
	return false;
}
